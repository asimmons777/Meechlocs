# Copy to .env and fill values

# Required
DATABASE_URL="mysql://meech:meechpass@127.0.0.1:3307/meechlocs"
JWT_SECRET=your_jwt_secret_here

# API server
PORT=4000
NODE_ENV=development

# Frontend base URL (used for Stripe success/cancel URLs and links)
APP_URL=http://localhost:5173

# CORS
# Comma-separated list of allowed origins (recommended in production)
# Example: CORS_ORIGIN=https://meechlocs.com,https://www.meechlocs.com
CORS_ORIGIN=

# Email (SendGrid)
# In production, verification endpoints fail if SENDGRID_* is not configured.
SENDGRID_API_KEY=
# Provide either SENDGRID_FROM_EMAIL or SENDGRID_FROM
SENDGRID_FROM_EMAIL=
SENDGRID_FROM=

# Auth
# When true, users must verify email before logging in.
REQUIRE_EMAIL_VERIFICATION=true

# Stripe (optional for now; required for real deposits/refunds)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
